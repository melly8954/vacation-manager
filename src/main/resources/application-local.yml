server:
  port: 8081

spring:
  servlet:
    multipart:
      max-file-size: 10MB     # 단일 파일 최대 업로드 크기
      max-request-size: 20MB  # 전체 요청(여러 파일 합산) 최대 크기
  datasource:
    driver-class-name: com.p6spy.engine.spy.P6SpyDriver
    url: jdbc:p6spy:mysql://localhost:3306/vacation_manager?characterEncoding=UTF-8&serverTimezone=Asia/Seoul
    username: vm1234
    password: 1q2w3e4r!

file:
  access-url-base: http://localhost:8081/files/
  storage-path: C:/vacation_manager/files/
  directories:
    evidence: evidence_files

